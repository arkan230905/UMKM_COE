=== INSTRUKSI CLEANUP DATA PEMBAYARAN BEBAN ===

CARA 1: Menggunakan phpMyAdmin/phpMyAdmin
----------------------------------------
1. Buka phpMyAdmin
2. Pilih database eadt_umkm
3. Jalankan query berikut:

-- Hapus jurnal umum untuk pembayaran beban
DELETE FROM jurnal_umum 
WHERE tipe_referensi = 'pembayaran_beban' 
AND user_id = 1;

-- Soft delete pembayaran beban
UPDATE pembayaran_bebans 
SET deleted_at = NOW() 
WHERE user_id = 1;

CARA 2: Menggunakan MySQL Command Line
----------------------------------------
1. Buka Command Prompt/CMD
2. Masuk ke direktori project: cd c:\UMKM_COE
3. Jalankan perintah:
   mysql -u root -p eadt_umkm < cleanup_pembayaran_beban_simple.sql

CARA 3: Menggunakan Laravel Tinker
----------------------------------------
1. Buka Command Prompt/CMD
2. Masuk ke direktori project: cd c:\UMKM_COE
3. Jalankan perintah:
   php artisan tinker
4. Ketik perintah berikut di tinker:

use Illuminate\Support\Facades\DB;

// Hapus jurnal umum untuk pembayaran beban
DB::table('jurnal_umum')
    ->where('tipe_referensi', 'pembayaran_beban')
    ->where('user_id', 1)
    ->delete();

// Soft delete pembayaran beban
DB::table('pembayaran_bebans')
    ->where('user_id', 1)
    ->update(['deleted_at' => now()]);

echo "Cleanup pembayaran beban selesai";

VERIFIKASI HASIL
-----------------
-- Cek data tersisa
SELECT COUNT(*) as total_pembayaran_beban 
FROM pembayaran_bebans 
WHERE user_id = 1 AND deleted_at IS NULL;

SELECT COUNT(*) as total_jurnal_pembayaran_beban 
FROM jurnal_umum 
WHERE tipe_referensi = 'pembayaran_beban' 
AND user_id = 1;

=== SELESAI ===
